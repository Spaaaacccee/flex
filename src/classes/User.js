export default class User {
    
    /**
     * 
     * @static
     * @param  {User} user 
     * @return {void}@memberof User
     */
    static updateDetails(user) {

    }

    uid;
    projects;
    constructor(uid) {
        this.uid = uid;
    }
}