{
  "rules": {
    ".read": "auth !=null",
    ".write": "auth != null",
    "users": {
      ".indexOn": "email",
      "$uid": {
        ".write": "$uid===auth.uid"
      }
    },
    "messages": {
      "$messengerID": {
        "messages": { ".indexOn": "timeSent" }
      }
    }
  }
}
